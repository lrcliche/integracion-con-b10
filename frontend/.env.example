# Modo recomendado (Docker y local con proxy Vite): peticiones relativas
VITE_API_URL=

# Modo directo (solo si el backend expone CORS)
# VITE_API_URL=http://localhost:8080

# Solo en Docker Compose (runtime del contenedor frontend, no se embebe en el bundle)
# VITE_PROXY_TARGET=http://backend:8080
